# WorkSight Desktop Client

PySide6 front-desk terminal for face check-in/out, employee registration and deletion.

## Setup
```
python -m venv .venv
.venv\Scripts\activate      # macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
python main.py
```
